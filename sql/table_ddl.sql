CREATE TABLE orders_cleaned (
   Order_ID VARCHAR(20),
   Order_Date DATE,
   Ship_Date DATE,
   Customer_ID VARCHAR(20),
   Segment VARCHAR(30),
   Country VARCHAR(50),
   Region VARCHAR(30),
   Product_ID VARCHAR(30),
   Category VARCHAR(50),
   Sales FLOAT,
   Quantity INT,
   Discount FLOAT,
   Profit FLOAT,
   Profit_Margin_PCT FLOAT
);
